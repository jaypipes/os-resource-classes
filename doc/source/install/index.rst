============
Installation
============

At the command line::

    $ pip install os-resource-classes

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv os-resource-classes
    $ pip install os-resource-classes
